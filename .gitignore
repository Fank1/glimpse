.DS_Store
__pycache__/
dist/
test-books/
new-test-books/
epub-corpus
books-from-vm/
analysis-report.txt
builder/fixture.epub
builder/fixture_extracted/
.claude/
.previews/

# Local README/Reddit media (hosted as GitHub release assets, not committed)
for-readme-and-reddit/

# Local scratch (browser MCP output, Figma frame grabs)
.playwright-mcp/
figma-*.png

# Drop-box for design exports; the ones in use are copied into plugin/assets/
icons-to-use/

# Local research/data archives — never belongs in the repo (e.g. reddit-dump.zip)
*.zip

# Compiled translation catalogues — rebuilt from .po at release time
plugin/l10n/*/*.mo

# Local Crowdin project id (see ~/.config/crowdin/token.env for the token)
.crowdin.env

# QA sweep output (screenshots + findings), regenerated per run
qa/
